# Test Environment Configuration
# This file is loaded by tests to provide consistent test values
# DO NOT use production values here

# Mock testing (default for tests)
IMAP_ADAPTER=mock
IMAP_HOST=localhost
IMAP_PORT=143
IMAP_USER=test@example.com
IMAP_PASS=testpass

# RustyMail REST API Server Configuration (test ports)
REST_HOST=127.0.0.1
REST_PORT=9437

# RustyMail SSE Server Configuration (test ports)
SSE_HOST=127.0.0.1
SSE_PORT=9438

# Dashboard Configuration (test ports)
DASHBOARD_ENABLED=true
DASHBOARD_PORT=9439
DASHBOARD_HOST=127.0.0.1

# Logging Configuration
LOG_LEVEL=debug

# Security Configuration
REQUIRE_HTTPS=false

# API Authentication (test key only!)
RUSTYMAIL_API_KEY=test-rustymail-key-2024

# Database Configuration (in-memory for tests)
CACHE_DATABASE_URL=sqlite::memory:

# MCP Stdio Proxy Configuration
MCP_BACKEND_URL=http://localhost:9437/mcp
MCP_TIMEOUT=30

# Cache configuration for tests
CACHE_MAX_MEMORY_ITEMS=100
CACHE_MAX_SIZE_MB=10
CACHE_MAX_EMAIL_AGE_DAYS=7
CACHE_SYNC_INTERVAL_SECONDS=60
