# Dependabot version updates are DISABLED.
# Dependency upgrades are managed manually via:
#   cargo update && cargo audit    (Rust)
#   npm update && npm audit        (frontend)
#
# Security alerts (CVE notifications) are still active via GitHub repo settings
# under Settings > Security > Dependabot alerts. Those do NOT require this file.
#
# To re-enable version update PRs, uncomment the config below.

version: 2
updates: []
