#[cfg(test)]
mod tests {
    use super::*;
    use crate::imap::types::MailboxInfo;
    use tokio::test;

    #[test]
    async fn test_imap_client_basic() {
        // Add tests here when needed
    }
} 